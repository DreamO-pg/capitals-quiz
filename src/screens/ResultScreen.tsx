import { Placeholder, Screen } from '../components/Screen';

export function ResultScreen() {
  return (
    <Screen>
      <Placeholder title="Итог раунда" hint="Этап 6: счёт кольцом, разбор ошибок, повтор." />
    </Screen>
  );
}
